"use strict";

var _lodash = require("lodash");

var _hooks = require("@wordpress/hooks");

var _blocks = require("@wordpress/blocks");

var _components = require("../components");

/**
 * External dependencies
 */

/**
 * WordPress dependencies
 */

/**
 * Internal dependencies
 */
function setDefaultCompleters() {
  var completers = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];
  var blockName = arguments.length > 1 ? arguments[1] : undefined;
  // Provide copies so filters may directly modify them.
  completers.push((0, _lodash.clone)(_components.userAutocompleter)); // Add blocks autocompleter for Paragraph block

  if (blockName === (0, _blocks.getDefaultBlockName)()) {
    completers.push((0, _lodash.clone)(_components.blockAutocompleter));
  }

  return completers;
}

(0, _hooks.addFilter)('editor.Autocomplete.completers', 'editor/autocompleters/set-default-completers', setDefaultCompleters);
//# sourceMappingURL=default-autocompleters.js.map