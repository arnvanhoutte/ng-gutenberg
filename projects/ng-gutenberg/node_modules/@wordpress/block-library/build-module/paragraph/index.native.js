import _objectSpread from "@babel/runtime/helpers/esm/objectSpread";

/**
 * External dependencies
 */
import { isEmpty } from 'lodash';
/**
 * WordPress dependencies
 */

import { __ } from '@wordpress/i18n';
import { create } from '@wordpress/rich-text';
/**
 * Internal dependencies
 */

import { settings as webSettings } from './index.js';
export { metadata, name } from './index.js';
export var settings = _objectSpread({}, webSettings, {
  __experimentalGetAccessibilityLabel: function __experimentalGetAccessibilityLabel(attributes) {
    var content = attributes.content;

    var plainTextContent = function plainTextContent(html) {
      return create({
        html: html
      }).text || '';
    };

    return isEmpty(content) ? __('Empty') : plainTextContent(content);
  }
});
//# sourceMappingURL=index.native.js.map